class ChangeCheckBoxVal{
   int dayIndex;
   String muscle;
   int exerciseIndex;
   bool value;

   ChangeCheckBoxVal({
     required this.dayIndex,
     required this.muscle,
     required this.exerciseIndex,
     required this.value,
   });
}